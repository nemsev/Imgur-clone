<?php
  //временный файл
  include_once 'lib/connect.php';
  
  // выбираем коллекцию
  //$images = $db->images; 
  //$comments = $db->comments;
  //$admins = $db->admins;

  // создаем админа
  //$admins->insert(array( 'username' => 'Nemcev', 'password' => md5('uhjpysq95') ));

  // удаляем ее
  //$admins->remove();
  //$images->remove();
  //$comments->remove();
?>